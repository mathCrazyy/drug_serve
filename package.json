{
  "name": "drug-serve",
  "version": "1.0.0",
  "private": true,
  "description": "药品管理服务 - 前端和后端应用",
  "scripts": {
    "postinstall": "cd frontend && npm install",
    "build": "cd frontend && npm install && npm run build",
    "dev": "cd frontend && npm run dev",
    "preview": "cd frontend && npm run preview"
  },
  "engines": {
    "node": ">=18.0.0",
    "npm": ">=9.0.0"
  }
}

